export enum PATH {
    HOME = '/',
    CHARACTERS = '/characters',
    CHARACTERS_CARD = '/characters/:id',
    LOCATIONS = '/locations',
    LOCATION_CARD = '/locations/:id',
    EPISODE = '/episode',
    EPISODE_CARD = '/episode/:id',
}