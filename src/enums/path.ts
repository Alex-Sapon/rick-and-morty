export enum PATH {
    HOME = '/',
    CHARACTERS = '/characters',
    CHARACTERS_CARD = '/characters/:id',
    LOCATIONS = '/locations',
    LOCATION_CARD = '/location/:id',
    EPISODES = '/episodes',
}